<Window x:Class="Diplom.AddTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить тест" Height="350" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Название теста:" FontSize="14" FontWeight="Bold"/>
        <TextBox x:Name="TestNameBox" Grid.Row="1" Width="400"/>

        <TextBlock Text="Варианты ответов:" Grid.Row="2" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>
        <ListBox x:Name="AnswersListBox" Grid.Row="3" Height="100" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox x:Name="NewAnswerBox" Width="300" Height="25"/>
            <Button Content="Добавить" Click="AddAnswer_Click" Width="80" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Сохранить тест" Click="SaveTest_Click" Width="120"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="80" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
