<Window x:Class="Diplom.AdminAddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление пользователя" Height="400" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Имя:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="NameBox" Grid.Row="0" Grid.Column="1" MaxLength="11" Width="200" PreviewTextInput="NameBox_PreviewTextInput"/>

        <TextBlock Text="Фамилия:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="SurnameBox" Grid.Row="1" Grid.Column="1" MaxLength="11" Width="200" PreviewTextInput="NameBox_PreviewTextInput"/>

        <TextBlock Text="Логин:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="LoginBox" Grid.Row="2" Grid.Column="1" MaxLength="11" Width="200"/>

        <TextBlock Text="Пароль:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <Grid Grid.Row="3" Grid.Column="1">
            <TextBox x:Name="PasswordTextBox" Width="170" Visibility="Collapsed"/>
            <PasswordBox x:Name="PasswordBox" Width="170"/>
            <Button Content="👁" Width="30" Height="25" HorizontalAlignment="Right" Click="TogglePasswordVisibility"/>
        </Grid>

        <TextBlock Text="Роль:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="RoleComboBox" Grid.Row="4" Grid.Column="1" Width="200"/>

        <TextBlock Text="Группа:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="GroupComboBox" Grid.Row="5" Grid.Column="1" Width="200"/>

        <Button Content="Добавить пользователя" Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" 
                HorizontalAlignment="Center" Width="200" Margin="10" Click="AddUser_Click"/>
    </Grid>
</Window>
