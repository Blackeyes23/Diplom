<Window x:Class="Diplom.Admin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Diplom"
        mc:Ignorable="d"
        Title="Admin"
        Height="450" Width="800"
        MinHeight="400" MinWidth="600"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Background="SkyBlue">

    <Border BorderBrush="Blue" BorderThickness="10" Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Фото пользователя -->
            <Image x:Name="UserPhoto" Source="Resources/DefaultUser.png" Width="150" Height="150"
                   Stretch="UniformToFill" Margin="10" Grid.Column="0"/>

            <!-- Информация -->
            <Grid Grid.Column="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Имя:" FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="NameText" FontSize="16" Margin="100,0,0,0"/>

                <TextBlock Text="Фамилия:" FontSize="16" FontWeight="Bold" Grid.Row="1"/>
                <TextBlock x:Name="SurnameText" FontSize="16" Grid.Row="1" Margin="100,0,0,0"/>

                <TextBlock Text="Группа:" FontSize="16" FontWeight="Bold" Grid.Row="2"/>
                <TextBlock x:Name="GroupText" FontSize="16" Grid.Row="2" Margin="100,0,0,0"/>

                <TextBlock Text="Роль:" FontSize="16" FontWeight="Bold" Grid.Row="3"/>
                <TextBlock x:Name="RoleText" FontSize="16" Grid.Row="3" Margin="100,0,0,0"/>
            </Grid>

            <!-- Кнопки -->
            <StackPanel Grid.Column="1" Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Добавить пользователя" Click="OpenAddUserWindow" Height="40" Width="180" Margin="10"/>
                <Button Content="Выйти" Click="Button_Click" Height="40" Width="120" Margin="10"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
